package interfacePackage;

interface ShapeInterface {
	
	// 5. methods in interface are abstract by default
	public int calculatePerimeter(int length, int breadth);
	public int calculateArea(int length, int breadth);
	
}
